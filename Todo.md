Getting the regisiter modal to work when click.
Use an onClick to have the register modal to open when clicking

Step 1. The register button should open the registrer modal
Step 2. The register modal form should call the register API function that is in auth.js

Login Modal

Step 1. The login Modal form should call the the API function in login function in auth.js
Step 2. Once the login is successful, save the token to local storage, and change the "isLogin" state to true. Look for isLogin, or logged in state variable

Step 3. Once changed to true, it should show the users name and avatar. And it should also allow me to see the profile route.
Step 4. Once successfully logged in, I should be able to add clothing items.